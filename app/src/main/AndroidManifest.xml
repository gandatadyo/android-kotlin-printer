<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exampleprint">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ConfirmationInvoice"
            android:label="Print Invoice"/>
        <activity
            android:name=".ReportTransaction"
            android:label="Laporan Transaksi" />
        <activity
            android:name=".InputInvoiceProduct"
            android:label="Pilih Barang Yang Disewa" />
        <activity
            android:name=".InputInvoice"
            android:label="Buat Transaksi" />
        <activity
            android:name=".InputCustomer"
            android:label="Input Customer" />
        <activity
            android:name=".InputProduct"
            android:label="Input Barang" />
        <activity
            android:name=".MasterCustomer"
            android:label="Master Customer" />
        <activity
            android:name=".MasterProduct"
            android:label="Master Barang" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".PrintActivity" />
        <activity android:name=".utils.DeviceListActivity"
            android:label="List Perangkat"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>